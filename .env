# MySQL Railway (folosit de Drizzle/ORM sau mysql2)
DATABASE_URL=mysql://{MYSQLUSER}:{MYSQLPASSWORD}@{MYSQLHOST}:{MYSQLPORT}/{MYSQLDATABASE}

# sau dacă codul tău folosește variabile separate
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_USER=${MYSQLUSER}
DB_PASS=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}

# Alte variabile
JWT_SECRET=a_secure_random_secret_string_12345
NODE_ENV=production
